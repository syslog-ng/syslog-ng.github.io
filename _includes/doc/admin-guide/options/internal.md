## internal()

*Description:* To collect warnings, errors and notices from {{ site.product.short_name }} itself, include this source in one of your source statements.

Example format in configuration:

```config
source s_local { internal(); };
```

For more details, see internal: Collecting internal messages.